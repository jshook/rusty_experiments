[package]
name = "simdbench"
version = "0.1.0"
edition = "2024"

[dependencies]
simsimd = "6"

[dev-dependencies]
criterion = { version = "0.5", features = ["html_reports"] }

[[bench]]
name = "svd_bench"
harness = false
